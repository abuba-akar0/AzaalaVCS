# Azaala VCS Database Configuration
# MySQL connection properties

mysql.url=jdbc:mysql://localhost:3306/azaala_vcs
mysql.username=root
mysql.password=@Rakabuba2274

# HikariCP Connection Pool Configuration
hikari.maximumPoolSize=10
hikari.minimumIdleConnections=2
hikari.connectionTimeout=30000
hikari.idleTimeout=600000
hikari.maxLifetime=1800000

# Hybrid Storage Configuration
hybrid.storage.enabled=true
hybrid.storage.content.database=false

# Application Settings
audit.logging.enabled=true
database.auto.init=true
logging.level=INFO
logging.queries.enabled=false

